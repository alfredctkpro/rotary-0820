# ⚠️ 這是 PUBLIC repo — 進 git 的東西全世界看得到，且 history 刪不掉
# 下面每一條都是「不小心 commit 就會出事」的東西

# ── 影片方針（8/13 更新：成品 mp4 進 git、由 GitHub Pages 網址播放）
# 只進轉好的 H.264 .mp4 成品；原始檔（.mov 大檔、未剪素材）仍不進
*.mov
*.m4v
*.avi
*.mkv

# ── 敏感素材：去識別化「之前」的原始檔一律不進
# 命名慣例：檔名含 -raw / -原始 / -未遮 的都擋掉
*-raw.*
*-原始.*
*-未遮.*
**/raw/
**/未遮蔽/

# ⚠️ 手機原始照片（HEIC）一律不進——通常是未遮蔽的實拍素材，且帶 GPS EXIF。
# 例：assets/real-calendar.HEIC 是爸媽月曆實拍的原檔，
#     進 repo 的是清過 EXIF 的 real-calendar.jpg。
*.heic
*.HEIC

# ⚠️ 生圖用的個人原始照——只是 gen-images 的參考素材，投影片並未引用，
#    沒有理由把原始個人照永久公開（成品 multiverse-me.png 才進 repo）。
assets/alfred.jpg
assets/alfred-face.*

# 電影海報原檔＝有版權的宣傳素材，只當生圖的構圖參考，不進 public repo。
# 產出的 multiverse-me.png 是致敬構圖＋Alfred 本人的臉，才是進 repo 的成品。
assets/movie-poster.jpg

# ── 投資相關（Demo 3 素材含真實債券部位金額，絕不進 public）
**/investiment-base/
**/對帳單*
**/statement*
*持倉*
*部位明細*

# ── 憑證與設定
.env
.env.*
!.env.example
*.pem
config.yaml
credentials*
# ⚠️ 不要寫 `*.key`——Keynote 投影片副檔名也是 .key，會把 slides/ 全擋掉。
# 憑證改用明確路徑或 *-private.key 這種帶後綴的寫法。
*-private.key
*_rsa
id_rsa*

# ── Hermes / LINE 設定（含 Channel Secret、Access Token）
**/hermes-config*
**/line-config*
*channel_secret*
*access_token*

# ── 系統與編輯器
.DS_Store
.AppleDouble
Thumbs.db
.idea/
.vscode/
*.swp

# ── Keynote 暫存
*.key.sb-*


# 工具暫存
.playwright-mcp/
.memsearch/
