# CLAUDE.md

> 本 repo 全程使用**繁體中文（台灣）**：文件、投影片內容、commit message 皆是。

## 這個 repo 是什麼

2026-08-20（四）18:30 扶輪社演講《貓的 AI 多重宇宙》——講者 Alfred（康貓 PP），40 分鐘，聽眾約 40 位企業主／高階主管（碰過 AI 的不到 10 人）。台北市新世代扶輪社「新世代 AI 工作坊三部曲 EPISODE 1」，華山町餐酒館，**現場用簡報筆**。

**沒有程式碼、沒有 build／test／lint。** 成品是根目錄那份**單檔自足的 38 頁 `index.html`** ＋四支 demo 影片。驗證方式＝用瀏覽器翻一遍。

⚠️ **public repo**，GitHub Pages 已啟用：`https://alfredctkpro.github.io/rotary-0820/` 就是演講網址。進 git 的東西全世界看得到，history 刪不掉。

**投影片已完成**，剩下的是排練與微調，不是重做。

| 檔案 | 用途 |
|---|---|
| `index.html` | 成品。改任何一頁直接改這裡 |
| [題詞跟轉場.md](題詞跟轉場.md) | **現場提詞卡**——逐頁重點＋按鍵節奏＋轉場句。Alfred 台上看這份 |
| [大綱與素材.md](大綱與素材.md) | **完整話術**——演練時對照用 |

**頁碼一律以 `index.html` 為準（38 頁）**，三份檔案已對齊。

## 🚨 紅線（public repo，不可越線）

- **絕對金額不出現**——投資段只能有百分比與匯率警戒線。
- **家人名字不出現**——太太與孩子的名字一律遮（「15:30 鼓課」這種可保留）。
- **安全故事（P21／P22）講現象**，不報精確數字、不出當事人名。
- `.gitignore` 已擋：投資部位、家人原始檔、生圖用的個人照與電影海報原檔、Hermes／LINE 金鑰。**不要寫 `*.key`**（Keynote 副檔名也是 `.key`）。

## 改投影片時要知道的

**風格**：白底高橋流，每頁＝**大字主張＋一行白話補述**，不做 bullet 牆。Token：暖紙白 `#FBFAF7`、藍墨 `#2f4fb0`、Noto Sans TC（900/700/500/400）。右側留白 `padding-right:148px`。

**全白底是定案**——餐酒館環境光關不掉，深底會糊成灰。**例外只有 P2／P37**：直式海報＋深藍底 `#0C1B44`，P37 用 `.rev` 鏡像與 P2 形成首尾書擋。⚠️ 海報**必須維持直式**（裁成橫的就認不出那部電影）。深色頁要在 `<section>` 掛 `.dark`，JS 才會把導覽轉淺色。

**三層主視覺**在 P13、P35 出現兩次——同一張圖、不同高亮，別畫兩張。三層名詞一律帶中文：Prompt（提示詞）／Context（情境）／Harness（駕馭）；大字頁的 Context 用完整版「（情境／來龍去脈）」，三層圖內用短版避免與右欄撞字。

**單檔自足**：CSS/JS 全內嵌，圖與 QR 存 `assets/`，不外連。

**生圖**：`node scripts/gen-images.mjs --force <名稱>`（模組與金鑰路徑會自動探測既有專案，圖內絕不含文字）。

### 🎮 簡報筆邏輯（已實作，改 JS 前先懂）

簡報筆只送 `PageDown`／`PageUp`，**按不到影片按鈕**。現行解法：

1. **頁內分段 `step` 狀態機**——影片頁：▶ 播放 → ▶ 關閉留在原頁 → ▶ 才翻頁。**P28 為三段**（多一段浮現檔案清單）。
2. **鐵律：影片播完不自動翻頁**——四個影片頁播完都還有話術要在同一頁講。
3. 已攔 `F5`（簡報筆可能送 F5 導致重載跳回第 1 頁）；`b`／`.` 黑屏。
4. **播放中不鎖死鍵盤**——簡報筆沒有 Esc，播放中按翻頁鍵＝關閉影片。
5. 上一頁鍵倒退 step；翻頁後 `document.body.focus()`。
6. 網址 `#N` 可直接跳頁（排練用）。

### 字體風險（當天必做）

字體走 Google Fonts 線上載入，**斷網會掉回系統字體、大字明顯變細**。上台前必須連網把投影片完整開一次讓瀏覽器快取；換瀏覽器或清快取就失效。

## 敘事骨幹（改任何一頁前要懂）

主線：**別人在學招式，我在練心法**（P4 點題 · P24 轉折 · P35-37 收束）。

比喻三件套：**多重宇宙**（人生角色）×**分身**（AI Agent，孫悟空拔毫毛，P4 埋伏筆／P23 回收／P33 再收）×**心法**（三層架構）。

| 宇宙 | 頁 | 場景 | 兌現的零件 |
|---|---|---|---|
| 一 | P5-P11 | 爸媽的 LINE | 全套入職（Harness） |
| 二 | P25-P28 | 全家週末旅遊 | Workflow（每週交卷） |
| 三 | P29-P34 | 投資決策 | Agent＋Skill（圓桌） |

**P20 是三層合一的機器人插圖**（滿版，三個標籤逐一浮現＝耳朵嘴巴／桌上文件／手腳工具）。

**兩個安全故事在 P21／P22**（三層講完之後，不是 Prompt 之後）：P21 信箱清空＝第二層沒給夠，P22 插隊＝第三層發過頭。所以 **P19 工具牆不可爆雷**，門禁卡那句只鋪墊、不講後果。

**誠實原則**：投影片不寫死任何影片引言（「這些見解我沒想過」是萬用收束句）；話術用「我只要說一句」，不說「全自動」。

**P34 證據鏈**：頁面大字＝那行 `/roundtable` 指令，與影片裡打進去的一字不差。

## 工作慣例

本 repo 由 Claude 直接 commit＋push，commit message 繁中。演講後待辦：講稿類檔案是否轉私有、`~/thinkr` MEMORY 瘦身、圓桌議事錄結論回寫 `2_decisions`。
