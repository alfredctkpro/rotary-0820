# instruction.md — 投影片製作交接（新 session 從這裡開始）

> **你的任務**：為 2026-08-20（四）扶輪社演講《貓的 AI 多重宇宙》製作 **36 頁 HTML 投影片**（單檔自足的 `index.html`，放本 repo 根目錄）。
> **唯一內容依據**＝同資料夾的 [`大綱與素材.md`](大綱與素材.md)（逐頁骨架＋每頁話術＋視覺指示，36 頁全寫好了，照做即可，**不要重新發明內容**）。
> **時程**：8/14（五）下班前要能全套演練——投影片今天就要能用。

---

## 一、動手前先讀（順序）

1. **`大綱與素材.md`**（本資料夾）——36 頁逐頁規格，這是製作藍圖
2. **`~/ctkpro-slides/CLAUDE.md`**——投影片工法總綱（設計 token、版面規則、元件庫、demo 影片播放層）
3. **`~/ctkpro-slides/bids/yilan-bids/index.html`**——**複製這個檔當骨架**（1280×720 `#stage` 縮放舞台、`playDemos()` 影片播放層、左右 1/6 點擊翻頁、播放中鎖翻頁、鍵盤導覽、進度條——全部現成，照搬）
4. **`~/bni-ai-news/ctkpro-intro/index.html`**——白底高橋流的風格基準（大字、極簡、一頁一重點）
5. **`~/bni-ai-news/2026-07-31/index.html`**——最近一場對外分享成品（20 頁，逐行浮現、大字對比等手法可抄）

## 二、風格決策（本場已定案，別翻案）

- **白底高橋流＋每頁兩層**：大字主張（一頁一個意思）＋一行白話補述（小一號、灰階——給分心回神的人的重新掛上點）。**不做 bullet 牆**，需要列點的頁最多三條。
- **Token**：白底、藍墨強調 `#2f4fb0`（BNI 基準）、Noto Sans TC（900/700/500/400）。yilan-bids 骨架的 token 結構沿用、色票換白底系。
- **三層主視覺**（Prompt 2024→Context 2025→Harness 2026 三階圖）在 P12、P33 重複出現——同一張圖、不同高亮，別畫兩張。
- **手繪插圖三張**走 gen-images 管線（藍墨手繪＋暖紙風，腳本參考 `~/bni-ai-news/ctkpro-intro/scripts/gen-images.mjs`，API key 重用 `~/ctkpro_blog_posts/.env`）：`universe-circles`（P3）、`desk-overflow`（P17）、`monkey-clones`（P21）。圖內**絕不含文字**。
- **P25 的 Demo 2 靜態素材不用截圖**——做成「文件卡片」（檔名標頭＋原文引用＋表格原樣），來源檔在 `~/thinkr/2_family/trips/`（對照表見大綱的「Demo 2 截圖清單」段）。**遮名規則：孩子與太太的名字一律不出現**（「15:30 鼓課」保留即可）。
- 對外簡報右側留白 `padding-right:148px`、右上角 CTK Pro logo 對齊內容右緣（工法見 ctkpro-slides CLAUDE.md）。

## 三、影片狀態（`demos/`，playDemos 用相對路徑）

| 檔案 | 狀態 | 用在 |
|---|---|---|
| `demos/demo2/demo2.mp4` | ✅ 已進 repo | P26（trip 實跑） |
| `demos/demo3/demo3-1-explain-investiment-base.mp4` | ✅ 已進 repo | P30（庫導覽） |
| `demos/demo3/demo3-2-call-a-roundtable-meeting.mp4` | ✅ 已進 repo | P32（圓桌實況） |
| `demos/demo1/demo1.mp4` | ✅ 已進 repo（原版 4:27，H.264 720p30） | P9 |

- **demo1 單一原版公開（8/13 Alfred 最終拍板）**：醫生名＝公開資訊、機器人 LINE ID 有 Messaging API 白名單鎖、就醫規律曝光經評估接受。曾做過的去識別化版（demo1-clean）已移除不用。**P9 按鈕單一 src、無 fallback**，四支影片做法一致。
- ⚠️ **P9 的實際素材與大綱描述不同**：大綱寫兩支影片（輸入端＋陪伴端），實際 Alfred 錄成**一支合一**（約 4.5 分鐘，含輸入→確認→GCal→晨間提醒→拜拜提醒）。按鈕做**一顆**即可；P9 話術的 cue 順序對應影片內段落。
- 影片方針（8/13 定案）：**H.264 mp4 成品進 git、GitHub Pages 網址播放**；原始 .mov 不進。新影片如需轉檔：`ffmpeg -i in.mov -c:v libx264 -preset medium -crf 23 -vf "scale=720:-2,fps=30" -pix_fmt yuv420p -c:a aac -b:a 128k -movflags +faststart out.mp4`。

## 四、發佈

- **GitHub Pages 已啟用**：`https://alfredctkpro.github.io/rotary-0820/`（main branch 根目錄）——`index.html` 放根目錄就是演講網址。
- **P36 謝幕頁的 QR code 指向上面這個網址**（QR 可用 api.qrserver.com 產生後下載存 `assets/`，別用外連——投影片單檔自足原則）。
- 本 repo 是 **public**：投影片內容＝可公開內容。**紅線：絕對金額不出現（投資段只有百分比與匯率線）、家人名字遮蔽。**（demo1 已於 8/13 拍板單一原版公開，四支影片全數在 repo。）

## 四之二、現場條件（8/13 由主辦海報 `assets/talk-ads.jpg` 確認）

| 項目 | 內容 |
|---|---|
| 主辦 | **台北市新世代扶輪社**（Alfred 為 PP／前社長，屬自家社） |
| 系列定位 | **「新世代 AI 工作坊 三部曲」EPISODE 1** |
| 時間 | 8/20（四）**18:30**（晚上場） |
| 地點 | **華山町餐酒館**（台北市中正區八德路一段 34 號） |
| 年度主題 | Rotary「CREATE LASTING IMPACT」 |

**已定案（8/13）**：

1. **全白底**——餐酒館環境光關不掉（聽眾要用餐），深底在環境光＋投影機亮度不足下會糊成灰。放棄與海報深藍宇宙風的視覺連貫，換取可讀性。沿用 ctkpro-slides 既有 token（暖紙白 `#FBFAF7`＋藍墨 `#2f4fb0`）。
2. **EPISODE 1 只標在 P1 封面**，收尾維持大綱原樣，**不預告 EP2／EP3**（後續兩集內容未定）。
3. **P1 不放講者頭銜**，只放「康貓 PP Alfred Kang」。（海報頭銜印為「數位生法實踐家」疑為誤植，不沿用。）
4. **社徽**：官網檔僅 126×200 且無去背，改**從海報左上角裁切去背**，成品 `assets/rcgn-logo.png`（162×125，含色塊＋社名）。P1 顯示約 110px 寬。需要更高品質再向社方索取向量檔。

### ⚠️ 投影片已成 37 頁——與大綱的頁碼有位移

8/13 在 P1／P2 之間插入一頁**意象頁**，因此 **`大綱與素材.md` 的 P3 以後，在 `index.html` 裡全部 +1**。

| 大綱 | index.html | 內容 |
|---|---|---|
| P1 | P1 | 封面 |
| — | **P2（新增）** | 意象頁：滿版 `multiverse-me.png`＋「無論在哪一個宇宙，你都有辦法搞定」 |
| P2 | P3 | 接梗頁（**改成三拍**：媽的多重宇宙 → 劃掉補「貓」 → 塞入「AI」 → tagline） |
| P3–P36 | P4–P37 | 其餘全部 +1 |

其他同批調整：

- **段落題**改為「**宇宙一／宇宙二／宇宙三**」＋場景名（原為超大 ①②③）——P5／P24／P28。
- **P11 Hermes** 加上 logo（`assets/hermes.png`，取自 `~/hermes-slides` 用過的 Nous Research 吉祥物），並把伏筆講清楚是**小龍蝦**不是 Hermes。
- **P32 圓桌名單**每席補上英文原名（Morgan Housel／Howard Marks／John Bogle／Nassim Taleb／Charlie Munger／Ray Dalio／Sam Zell），呂宗耀標「台灣投資前輩」。
- **P34 三層圖**改為由下而上逐層浮現（同 P13 做法）。
- **P36 收束**改為新文案：「多個宇宙，一個你／分身是 AI 變的，心法是你掌握的／你的每個場景，就是你的下個 AI 宇宙。」
- **P2／P36 版面＝直式海報＋深藍底 `#0C1B44`**（取自主辦海報的 `#011C47` 系）。⚠️ **必須維持直式**——曾做成 landscape 滿版，Alfred 反饋「裁成橫的之後認不出那部電影」，直式輪廓才是辨識點。P36 用 `.rev` 鏡像（圖右文左），與 P2 形成首尾書擋。
- 深色頁在 `<section>` 掛 `.dark`，JS 會同步在 `<body>` 掛 `.on-hero`，導覽與進度條自動轉淺色——否則深墨色頁碼會消失在圖上。**新增深色頁記得掛 `.dark`。**
- **P11 不再有「這個名字先記住」那句**（8/13 Alfred 指示整句捨棄）。伏筆改由話術口頭處理即可。

> ✅ **伏筆已閉環，不需要額外的 Hermes 頁**：P11 埋的是「**小龍蝦**」這個名字，P15 安全故事一（「幫我整理 email」→ 信箱清空）就是它的回收。Hermes 本身不需要再回收。

### 🖼 素材決策（8/13 定案，勿翻案）

| 素材 | 狀態 |
|---|---|
| `assets/real-calendar.jpg` | P5 用。爸媽牆上月曆實拍。**不做模糊**——Alfred 拍板：上面的人名都是可公開查到的醫師，非敏感資訊（與 demo1「醫生名＝公開資訊」同一標準）。⚠️ 但 **EXIF 一律清除**（原檔帶爸媽家 GPS 座標，與「醫師姓名公開」無關）。原始 `real-calendar.HEIC` 已被 `.gitignore` 擋下。 |
| `assets/alfred.jpg` | Alfred 正面照，供 P21 圖生圖當參考。無 GPS。 |
| `assets/desk-overflow.png` | ✅ P17 已產出（1400×781）。 |
| `assets/monkey-clones.png` | ✅ P21 已產出（1400×781）。以 `alfred.jpg` 圖生圖，Alfred 為猴王、吹出各拿工具的分身——**工具可在 P20「發工具」回頭指**。 |
| `assets/rcgn-logo.png` | 社徽，僅 P1 用。 |
| P3 插圖 | **改用純 CSS 圓圈版**（8/13 Alfred 拍板），不走 gen-images。 |

> 生圖腳本＝本 repo 的 `scripts/gen-images.mjs`（橫式構圖、支援參考圖）。**不是** `~/bni-ai-news/ctkpro-intro/scripts/gen-images.mjs`（那份是直式、輸出到別的目錄）。依賴與金鑰都借用既有專案，本 repo 不裝 node_modules。
> 產完記得 `magick <檔> -fuzz 3% -trim +repage <檔>` 裁掉 letterbox 補邊。

⚠️ **海報副標順序（LINE→投資→旅遊）與實際講序（LINE→旅遊→投資，投資壓軸）不同**——副標只是列舉不是議程，P1 照抄海報原句即可，P3 話術維持大綱的正確講序，**不要被海報帶偏**。

### 🎮 現場用**簡報筆**操作（8/13 Alfred 補充，影響鍵盤處理）

骨架 `yilan-bids/index.html` 的 `keydown` 已涵蓋 `PageDown`／`PageUp`（＝多數簡報筆送出的鍵），主功能可直接沿用。但**必須補三項**：

1. **播放中不可鎖死**：骨架在影片播放時除 `Esc`／空白外吃掉所有鍵，但**簡報筆通常沒有 Esc 鍵**——影片播到一半想跳過會卡住。改為：播放中按「下一頁」＝**關閉影片並留在原頁**（等同 Esc），再按一次才翻頁。
2. **攔截 `F5`**：部分簡報筆「開始放映」鍵送 F5，瀏覽器會**重新整理跳回第 1 頁**。必須 `preventDefault()`。順帶攔 `b`／`.`（黑屏鍵）。
3. **焦點保險**：翻頁後把焦點強制拉回舞台容器，避免點到影片元素後鍵盤事件收不到。

#### ⚠️ 最關鍵：簡報筆按不到「播放影片」按鈕

骨架的影片靠 `<button onclick="playDemos(...)">` 觸發**滑鼠點擊**，簡報筆只送 PageDown／PageUp，**按不到**。這是骨架為「站在筆電旁操作」設計的，與本場站台演講不合。

**解法＝讓「下一頁」鍵在影片頁分段生效**（`step` 狀態機，非直接翻頁）：

```
進入影片頁（顯示大字／指令）
  ├─ ▶ 第一次 ──→ 播放影片（全螢幕，不翻頁）
  ├─ 影片自然播完 ┐
  ├─ ▶ 播放中 ────┴→ 關閉影片，**留在原頁**（大字還在）
  └─ ▶ 再一次 ──→ 才真正翻到下一頁
```

**鐵律：影片播完一律不自動翻頁。** 四個影片頁播完後都還有話術要在同一頁講——P9 回收 P6 伏筆、P26 要切資料夾截圖、P32 要講「它會累積／我只出了一句話」且頁面那行指令必須留著當證據鏈（見大綱各頁 🎤）。

**P26 為頁內三段**（8/13 定案，維持總頁數 36、不拆頁）：

| step | 畫面 | 觸發 |
|---|---|---|
| 0 | 大字「每週四，交三個方案給我挑」 | 進頁 |
| 1 | 播 `demos/demo2/demo2.mp4` → 關閉留在本頁 | ▶ |
| 2 | 淡入 `trips/` 資料夾清單截圖（七份帶日期＝每週交卷的收據） | ▶ |
| → | 進 P27 | ▶ |

其餘三個影片頁（P9／P30／P32）為頁內兩段（播放 → 翻頁）。

- **滑鼠點按鈕的原有方式保留**當備援，兩條路都能觸發同一個 `playDemos()`。
- 上一頁鍵（PageUp）在頁內分段中應**倒退 step**，退到 step 0 再往前才翻前一頁。

### ✅ 上台前現場檢查清單

1. ⚠️ **先連網把投影片完整開一次**（8/13 定案的字體對策）——字體走 Google Fonts 線上載入，餐酒館斷網會掉回 PingFang TC，**大字會明顯變細、高橋流力道打折**。連網開過一次後瀏覽器會快取，之後斷網也還在。**換瀏覽器或清快取就失效，務必當天現場再開一次。**
2. 用**簡報筆**實測：上一頁／下一頁／**影片播放與跳出**／P26 三段切換。
3. 確認瀏覽器已全螢幕（`F`）。
4. 確認四支影片都能播（P9／P26／P30／P32）。

## 五、本 session 重要決策備忘（理解脈絡用，均已反映在大綱中）

1. **主線**：別人學招式、我練心法。比喻三件套互相咬合：多重宇宙（人生角色）×分身（Agent，孫悟空）×心法（三層 2024→2025→2026）。
2. **三個宇宙各自兌現 Harness 的不同零件**：宇宙一＝全套入職、宇宙二＝Workflow（每週交卷）、宇宙三＝Agent＋Skill（圓桌）。
3. **誠實原則**（務實老手人設的技術實作）：投影片不寫死任何影片引言（「這些見解我沒想過」是萬用收束句）；話術用「我只要說一句」不說「全自動」；安全故事講現象不報精確數字、不出當事人名。
4. **P32 證據鏈**：頁面大字＝那行指令，與影片 B 裡打進去的一字不差。
5. **P31 有一個 Alfred 待拍板**（頁面上有註記）：影片 A 若已完整介紹分身名單，P31 縮成 10 秒或併入 P30 口述。**先照大綱做全版**，他拍板後再調。
6. **時間可長可短**（晚上自家社場），節奏表當參考、砍法備而不用。

## 六、接下來的完整待辦（依序）

1. ✅ **投影片 `index.html` 36 頁**——已完成，逐頁實機渲染驗證（無溢出、無 JS 錯誤）
2. ✅ 插圖：`desk-overflow`（P17）、`monkey-clones`（P21）已產；P3 改用純 CSS 版
3. ✅ demo1 已進 repo（四支影片全數在位）
4. ⬜ **Alfred 待拍板**：P31 分身名單頁——若影片 A 已完整介紹過，可縮成 10 秒或併入 P30 口述（目前是全版）
5. ⬜ 影片 YT 未公開備援連結回填 `demos/demo*-拍攝腳本.md`
6. ⬜ 8/14 全套演練 ×3（投影片＋現場講影片 cue）——順便實測整場時間，影片總長 12:46，照現況全場約 44-45 分
7. ⬜ commit＋push（Alfred 指示：做完更多再推；推上去即為演講網址）
8. ⬜ 演講後：講稿類檔案是否轉私有、`~/thinkr` MEMORY 瘦身、圓桌議事錄結論回寫 `2_decisions`

> 工作慣例：本 repo 由 Claude 直接 commit＋push（與 investiment-base 的「AI 不主動 commit」不同）。commit message 繁中。
